name: Download YouTube Video

on:
  workflow_dispatch:
    inputs:
      url:
        description: 'YouTube video URL'
        required: true
      download_type:
        description: 'Download type'
        required: true
        type: choice
        default: 'video'
        options:
          - 'video'
          - 'audio'
      format:
        description: 'Custom format selector (video only, ignored for audio)'
        required: false
        default: 'bestvideo[height<=720]+bestaudio/best[height<=720]'

permissions:
  contents: write

jobs:
  download-and-upload:
    runs-on: ubuntu-latest
    steps:
      - name: Download cookies.txt via API (no clone)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -H "Authorization: token $GITHUB_TOKEN" \
               -H "Accept: application/vnd.github.v3.raw" \
               -o cookies.txt \
               "https://api.github.com/repos/${{ github.repository }}/contents/cookies.txt?ref=${{ github.ref_name }}"

      - name: Install Deno (JavaScript runtime)
        run: |
          curl -fsSL https://deno.land/install.sh | sh
          echo "$HOME/.deno/bin" >> $GITHUB_PATH

      - name: Install ffmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install yt-dlp
        run: pip install yt-dlp

      - name: Download video or audio
        run: |
          mkdir -p videos
          if [ "${{ inputs.download_type }}" = "audio" ]; then
            echo "Downloading best audio (m4a)"
            yt-dlp \
              --format bestaudio/best \
              --extract-audio \
              --audio-format m4a \
              --output "videos/%(id)s.%(ext)s" \
              --no-playlist \
              --cookies cookies.txt \
              --js-runtime deno \
              --remote-components ejs:github \
              "${{ inputs.url }}"
          else
            echo "Downloading video with format: ${{ inputs.format }}"
            yt-dlp \
              --format "${{ inputs.format }}" \
              --output "videos/%(id)s.%(ext)s" \
              --no-playlist \
              --cookies cookies.txt \
              --js-runtime deno \
              --remote-components ejs:github \
              "${{ inputs.url }}"
          fi

      - name: Split any file larger than 48 MB into chunks and gzip them
        run: |
          max_bytes=$((48 * 1024 * 1024))   # 48 MB
          for file in videos/*; do
            if [ -f "$file" ]; then
              size=$(stat -c%s "$file")
              if [ "$size" -gt "$max_bytes" ]; then
                echo "Splitting $file ($size bytes)"
                split -b 48M --numeric-suffixes=1 --suffix-length=2 "$file" "${file}.part"
                for chunk in "${file}.part"*; do
                  gzip "$chunk"   # each chunk is now .partNN.gz
                done
                rm "$file"        # remove the original large file
              fi
            fi
          done

      - name: Upload all files to repository via API (no git clone)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          for file_path in videos/*; do
            [ -f "$file_path" ] || continue
            filename=$(basename "$file_path")
            echo "Uploading $filename ..."

            content_base64=$(base64 -w 0 "$file_path")

            cat > payload.json <<EOF
          {
            "message": "Add ${{ inputs.download_type }}: ${{ inputs.url }}",
            "content": "${content_base64}",
            "branch": "${{ github.ref_name }}"
          }
          EOF

            curl -X PUT \
              -H "Authorization: token $GITHUB_TOKEN" \
              -H "Accept: application/vnd.github.v3+json" \
              "https://api.github.com/repos/${{ github.repository }}/contents/videos/${filename}" \
              -d @payload.json

            rm -f payload.json
            echo "Uploaded: $filename"
          done
