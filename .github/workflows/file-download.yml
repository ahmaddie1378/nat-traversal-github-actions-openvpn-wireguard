name: Download Blocked File (LFS)

on:
  workflow_dispatch:
    inputs:
      url:
        description: 'Direct URL of the file'
        required: true
      filename:
        description: 'Filename to save as (optional – leave empty to use name from URL)'
        required: false

jobs:
  fetch:
    runs-on: ubuntu-latest
    permissions:
      contents: write          # required to push LFS objects and commits
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Ensure git-lfs is installed and configured
      - name: Setup Git LFS
        run: |
          sudo apt-get update -y
          sudo apt-get install -y git-lfs
          git lfs install

      - name: Download the file
        run: |
          URL="${{ inputs.url }}"
          if [ -n "${{ inputs.filename }}" ]; then
            FILENAME="${{ inputs.filename }}"
          else
            FILENAME=$(basename "$URL" | cut -d '?' -f1)
          fi
          echo "Downloading $URL => $FILENAME"
          wget --timeout=30 -q -O "$FILENAME" "$URL" || curl -L --max-time 30 -o "$FILENAME" "$URL"
          echo "FILENAME=$FILENAME" >> $GITHUB_ENV

      # Track the downloaded file with LFS (creates/updates .gitattributes)
      - name: Track file with Git LFS
        run: |
          git lfs track "$FILENAME"
          # .gitattributes is modified – add it so it’s committed
          git add .gitattributes

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "$FILENAME"
          git commit -m "Download (LFS): $FILENAME" || echo "Nothing to commit"
          git push
